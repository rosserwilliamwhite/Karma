# Policy Gradients
## Algorithm
1. sample trajectory
2. $\nabla_\theta J(\theta)\approx\sum_i(\sum_t\nabla_\theta \log\pi_\theta(a_t^i|s_t^i))(\sum_t r(s_t^i,a_t^i))$
3. $\theta \text{ += } \alpha \nabla_\theta J(\theta)$

but how to get grad_th(log(pi))